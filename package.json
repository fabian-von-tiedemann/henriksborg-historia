{
  "name": "henriksborg-eleventy-decap",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "prebuild": "python3 -c \"import PIL\" 2>/dev/null || pip3 install --quiet pillow; python3 tools/generate_social_images.py; python3 tools/build_gallery_json.py",
    "build": "npm run prebuild && eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}